{% extends "vista.html.twig" %}

{% block body %}
    {{ form_start(formulario) }}
    {{ form_widget(formulario) }}
    {{ form_end(formulario) }}

    <a href="{{ url('crud_alumnos')}}">Volver al inicio</a>
{% endblock %}